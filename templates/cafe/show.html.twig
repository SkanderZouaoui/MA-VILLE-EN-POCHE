{% extends 'base.html.twig' %}

{% block title %}Cafe{% endblock %}

{% block body %}
    <h1>Cafe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cafe.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cafe.nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ cafe.image }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cafe.description }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ cafe.localisation }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ cafe.note }}</td>
            </tr>
            <tr>
                <th>Avis</th>
                <td>{{ cafe.avis }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cafe_index') }}">back to list</a>

    <a href="{{ path('cafe_edit', {'id': cafe.id}) }}">edit</a>

    {{ include('cafe/_delete_form.html.twig') }}
{% endblock %}
